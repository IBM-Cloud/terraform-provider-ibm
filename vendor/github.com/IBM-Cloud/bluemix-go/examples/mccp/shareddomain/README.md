# Shared Domain example

This example shows how to perform CRUD operation on cloud foundry shared domain.

This creates a shared domain which will be shared across all organization (admin-only). After successfull creation it retrives the domain and deletes domain.

Example: 

```
go run main.go
```




